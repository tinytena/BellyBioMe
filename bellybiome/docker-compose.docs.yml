services:
  docs:
    image: bellybiome_local_docs
    container_name: bellybiome_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./bellybiome:/app/bellybiome:z
    ports:
      - '9000:9000'
    command: /start-docs
